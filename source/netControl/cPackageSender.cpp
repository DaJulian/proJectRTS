#include "cPackageSender.h"

#define NET_TESTSIZE 4

int cPackageSender::InitNet(int sock,short int Owner)
{
/*    thisOwner = Owner;

    sockfd = sock;
    n = sockfd + 1;
    //Load the ReadFd to be focused on the corresponding socket
    FD_SET( sockfd ,&read_fd);

    //set waitingtime
    //waiting time very low
    tv.tv_sec = 0;
    tv.tv_usec = 50000;

    return 0;*/

}
